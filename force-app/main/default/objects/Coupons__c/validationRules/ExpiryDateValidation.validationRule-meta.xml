<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ExpiryDateValidation</fullName>
    <active>true</active>
    <description>Expiry Date should always be greater than today while creating the record</description>
    <errorConditionFormula>Expiry_Date__c &lt; TODAY()&amp;&amp; ISNEW()</errorConditionFormula>
    <errorDisplayField>Expiry_Date__c</errorDisplayField>
    <errorMessage>Expiry Date should always be greater than today while creating the record</errorMessage>
</ValidationRule>
